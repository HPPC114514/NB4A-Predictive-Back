# Predictive Back Progress

Last updated: 2026-05-24

| Area | File or Screen | Current Status | AndroidX Back Status | Risk | Next Step |
| --- | --- | --- | --- | --- | --- |
| Main activity shell | `app/src/main/java/io/nekohasekai/sagernet/ui/MainActivity.kt` | Updated in phase 1; unchanged in phases 2-3 | Uses lifecycle-aware `OnBackPressedCallback` for drawer and main fallback navigation | Needs device/emulator gesture verification | Verify drawer, configuration, and non-configuration main fragments on Android 13+ |
| Navigation drawer | `layout_main.xml` + `MainActivity.kt` | Updated in phase 1; unchanged in phases 2-3 | Drawer callback enabled only while drawer is visible | DrawerLayout may also have its own AndroidX handling; behavior should still be close-first | Test open drawer back press and predictive gesture |
| Configuration page | `ConfigurationFragment.kt` | Updated in phase 1; unchanged in phases 2-3 | Uses `ToolbarFragment.onBackPressed()` through the Activity callback to consume active search | Only toolbar search state was handled | Test search active, empty-focused search, and normal configuration back |
| Main fragment fallback | `GroupFragment`, `RouteFragment`, `SettingsFragment`, `WebviewFragment`, `ToolsFragment`, `LogcatFragment`, `AboutFragment` through `MainActivity.kt` | Updated centrally in phase 1 | Main callback returns these screens to configuration | Individual temporary states were not all migrated | Review only if device testing exposes skipped temporary state |
| Toolbar fragment base | `ToolbarFragment.kt` | Checked, not modified | Existing `onBackPressed(): Boolean` hook is reused | Not a lifecycle callback by itself; currently invoked from Activity callback | Consider future fragment-specific callback only if needed |
| Profile editor base | `ProfileSettingsActivity.kt` and subclasses | Updated in phase 2; unchanged in phase 3 | AndroidX callback enabled while `DataStore.dirty` is true | Needs emulator/device verification across representative protocol editors | Test dirty/clean back, save, discard, cancel, delete, and preference dialogs |
| Chain profile editor | `ChainSettingsActivity.kt` | Updated in phase 2; unchanged in phase 3 | Inherits profile editor callback; reorder/remove/add call `markProfileDirty()` | Direct list mutation must keep dirty callback in sync | Test reorder, remove, add/replacing profile, then back |
| Config editor | `ConfigEditActivity.kt` | Updated in phase 2; unchanged in phase 3 | AndroidX callback enabled while local editor `dirty` is true | JSON validation behavior must remain unchanged | Test invalid JSON save, dirty back save/discard/cancel, and clean back |
| Custom sing-box config profile | `ConfigSettingActivity.kt` | Updated in phase 2; unchanged in phase 3 | Inherits profile editor callback; preference listener calls `markProfileDirty()` | NB4A-specific config UI needs device verification | Test editing config preference then system back |
| Group settings | `GroupSettingsActivity.kt` | Updated in phase 2; unchanged in phase 3 | AndroidX callback enabled while `needSave()` is true | Subscription user-info preservation path needs regression testing | Test basic/subscription group dirty back, save, discard, cancel, delete |
| Route settings | `RouteSettingsActivity.kt` | Updated in phase 2; unchanged in phase 3 | AndroidX callback enabled while `needSave()` is true | Empty-route validation must remain visible on apply/save | Test dirty back, empty-route apply, save, discard, cancel, delete |
| Assets activity | `AssetsActivity.kt` | Updated in phase 3 | Legacy Activity `onBackPressed()` replaced with AndroidX finish callback | Asset deletion undo/import flows need device verification | Test direct back, toolbar up, import file, swipe delete/undo |
| VPN permission flow | `VpnRequestActivity.kt` | Updated in phase 3 | AndroidX finish callback for the transient request Activity | Must verify VPN permission accept/deny/back still maps to service result correctly | Test VPN permission accept, deny, and back/cancel |
| Profile selection temporary flow | `ProfileSelectActivity.kt` | Updated in phase 3 | AndroidX finish callback preserves no-result cancellation | Callers must still handle cancellation | Test back without selection from chain/route selection |
| Switch temporary flow | `SwitchActivity.kt` | Updated in phase 3 | AndroidX finish callback preserves dialog-style cancellation | Service reload path must remain selection-only | Test back without selection and selecting a profile |
| Scanner import flow | `ScannerActivity.kt` | Updated in phase 3 | AndroidX finish callback and toolbar up finish | Camera permission, QR parse, and file import need device verification | Test system back, toolbar up, camera permission deny, QR import, image import |
| Settings app manager | `AppManagerActivity.kt` | Updated in phase 3 | Default system back plus toolbar up finish | Live per-app proxy writes need regression testing | Test toolbar up/system back after search and selection changes |
| Tools STUN page | `StunActivity.kt` | Updated in phase 3 | Default system back plus toolbar up finish | Async STUN error dialog needs device verification | Test toolbar up/system back before/during/after test and error dialog |
| Tools host | `ToolsFragment.kt`, `NetworkFragment.kt`, `BackupFragment.kt` | Checked in phase 3 | Uses main Activity fallback; dialogs use AppCompat/Dialog behavior | ViewPager tab back behavior was not changed | Test tools tab, backup dialogs, reset confirm, import confirm/cancel |
| Settings host | `SettingsFragment.kt`, `SettingsPreferenceFragment.kt` | Checked in phase 3 | Uses main Activity fallback; preference dialogs use AppCompat/Dialog behavior | Preference dialogs and global custom config launch need verification | Test settings back, preference dialog back, global custom config editor |
| Dialog helper | `AlertDialogFragment.kt`, `QRCodeDialog.kt`, Material dialogs | Checked in phase 3 | Existing Dialog/AppCompat back handling | Gesture cancellation semantics not device-verified | Test representative confirmation, QR, error, and import progress dialogs |
| Background shortcuts | `QuickEnableShortcut.kt`, `QuickDisableShortcut.kt`, `QuickToggleShortcut.kt` | Checked in phase 3, not modified | Plain `android.app.Activity`; immediate background shortcut work | Converting may affect background process shortcut behavior | Leave unless testing shows a specific issue |
| Blank activity | `BlankActivity.kt` | Checked in phase 3, not modified | Plain `AppCompatActivity`; immediate finish after optional crash-log forwarding | Low; no user-facing intermediate state | Leave unless testing shows a specific issue |
| Manifest opt-in | `app/src/main/AndroidManifest.xml` | Checked, not modified | Contains `enableOnBackInvokedCallback=true` only | Manifest flag alone is insufficient | Keep as opt-in, do not claim complete support from this alone |
| SDK level | `buildSrc/src/main/kotlin/Helpers.kt` | Updated after phase 3 | Not a back handler, but now targets Android 16/API 36 for platform validation | Target SDK 36 can expose behavior changes outside predictive back | Keep `minSdk = 21`; use CI and device testing to separate SDK regressions from predictive-back regressions |
| Android CI workflow | `.github/workflows/android-ci.yml` | Added after phase 3 | Enforces AndroidX-first guardrails and docs presence before APK build | Workflow is new and still needs a successful full run after cache-key fixes | Run `Android CI` on GitHub and download `arm64-v8a-debug-apk` |
| CI static guardrails | `.github/workflows/android-ci.yml` | Fixed after false positive on `android:windowNoTitle` | Fixed-string scan for `OnBackInvokedCallback`, `OnBackAnimationCallback`, and `android.window` | Grep-based checks are intentionally conservative and may need precise excludes later | If it fails, inspect the exact match before changing app code |
| CI libcore cache | `.github/workflows/android-ci.yml` | Fixed after `hashFiles()` template validation failure | Not a back handler; required before Android compile can resolve `libcore.Libcore` | Cache key computation could still fail if file list is empty or shell behavior changes | Re-run `Android CI`; if libcore builds, keep using Actions as primary packaging path |
| Build verification | GitHub Actions `Android CI` | Preferred path after local Windows build blockers | Not a source-level back API issue | Local Windows build lacked SDK/Go/libcore; Actions must be the build gate for now | Use Actions artifact for manual device testing, then record tested Android versions and failures |
